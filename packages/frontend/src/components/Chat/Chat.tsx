import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
    width: 75%;
`;

export const Chat = () => (
    <StyledWrapper>
        Chat
    </StyledWrapper>
);
